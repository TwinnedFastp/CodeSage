# 前端环境变量示例
# 复制为 .env.local 后按需修改；未配置时使用默认值

# 后端 API 基础路径
# - Docker 部署：留空（走默认 /api/v1，由 nginx 反向代理到 backend）
# - 本地 npm run dev：留空（走默认 /api/v1，由 vite proxy 转发到 localhost:8000）
# VITE_API_BASE_URL=/api/v1
